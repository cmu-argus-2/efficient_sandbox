# arducam_spi_test

Small hardware test app for the ArduCAM Mini 2MP ArduChip SPI path only.

Checks:

- E1x SPI init on `SPI_2`
- ArduChip test register loopback
- ArduChip version register (`0x40`)
- FIFO reset command path

